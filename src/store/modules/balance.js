const name = "BALANCE";
const namespaced = true;
const state = {
  income:0,
  outcome:0,
};
const getters = {
  STATE: (s) => s,
};
const mutations = {};
const actions = {};
export default {
    name,
    namespaced,
    state,
    getters,
    mutations,
    actions,
  };